<!-- BEGIN:nextjs-agent-rules -->

# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` (resolved from this file's directory; in monorepos the `next` package may not be visible from the repo root) before writing any code. Heed deprecation notices.

This block is written and re-added by `next dev` — verify at `node_modules/next/dist/server/lib/generate-agent-files.js`. Removing it from a diff only re-creates the uncommitted change; committing it with your work keeps the tree clean.

<!-- END:nextjs-agent-rules -->

# Project Development Rules
# This is NOT the Next.js you know
# Next.js generated instructions


- Use Next.js App Router.
- Use TypeScript.
- Use Tailwind CSS.
- Prefer Server Components.
- Use Client Components only when necessary.
- Do not introduce Express.
- Do not introduce PostgreSQL.
- Do not introduce a database unless explicitly requested.
- Do not install unnecessary dependencies.
- Keep components modular.
- Maintain accessibility.
- Never use color as the only indicator.
- Respect prefers-reduced-motion.
- Optimize all images.
- Do not invent participant information.
- Do not invent sponsorship claims.
- Do not change the existing visual design without approval.
- Mobile responsiveness is mandatory.
- Do not modify unrelated files.
- Before making architectural or structural changes, inspect the existing project and relevant documentation.
- Do not rewrite working code unnecessarily.
- Implement changes incrementally and verify the build after major changes.
- Do not modify multiple unrelated sections in a single task.
- Preserve existing design tokens, spacing, typography, and component patterns.
- If requirements are ambiguous, identify the ambiguity before making a major implementation decision.