import { impactCategories, partnershipPillars, partnershipTiers } from "@/content";
import { SectionHeading } from "./SectionHeading";

export function Partners() {
  return <section id="partnerships" className="scroll-mt-24 bg-brand-charcoal py-20 text-white lg:py-28"><div className="container-site"><SectionHeading dark eyebrow="Partnership opportunities" title="Turn support into a national story." description="Partnership can strengthen training, open international opportunity, advance inclusion and carry Bangladesh’s representation to Helsinki." /><div className="mt-10 grid gap-4 md:grid-cols-2 lg:grid-cols-4">{partnershipPillars.map((pillar) => <article key={pillar.id} className="rounded-2xl border border-white/10 bg-white/5 p-5"><p className={`text-sm font-black tracking-[0.18em] ${pillar.iconColor}`}>{pillar.label}</p><p className="mt-4 text-sm leading-6 text-slate-300">{pillar.description}</p></article>)}</div><div className="mt-14 grid gap-3 sm:grid-cols-2 lg:grid-cols-4">{impactCategories.map((item) => <div key={item.number} className="rounded-xl border border-white/10 p-4"><span className="text-sm font-bold text-emerald-300">0{item.number}</span><p className="mt-2 font-semibold">{item.label}</p></div>)}</div><div className="mt-14 grid gap-5 md:grid-cols-2 lg:grid-cols-3">{partnershipTiers.map((tier) => <article key={tier.id} className={`rounded-2xl border p-6 ${tier.isHighlighted ? "border-emerald-400 bg-emerald-400/10" : "border-white/15 bg-white/5"}`}><p className="text-sm font-bold uppercase tracking-wide text-emerald-300">{tier.name}</p><p className="mt-4 text-sm leading-6 text-slate-300">{tier.description}</p>{tier.benefits.length ? <ul className="mt-5 space-y-2 text-sm text-slate-200">{tier.benefits.map((benefit) => <li key={benefit} className="flex gap-2"><span aria-hidden="true" className="text-emerald-300">✓</span>{benefit}</li>)}</ul> : null}<a href="#contact" className="mt-6 inline-block font-bold text-white underline underline-offset-4">Discuss this partnership</a></article>)}</div></div></section>;
}
